langchain-core
langchain-text-splitters
# -------------------------
# Core / Tests / Utils
# -------------------------
python-dotenv==1.0.1
pandas==2.2.3
pytest==8.3.3
beautifulsoup4==4.12.3
dotenv
pytest
# -------------------------
# LangChain & OpenAI (1.x series)
# Keep only top-level packages; avoid pinning langchain-core or text-splitters
# which caused dependency conflicts.
# -------------------------
langchain==1.1.3
langchain-openai==1.1.2

# Optional LangChain extension (community helpers) - install if needed
langchain-community==0.4.1

# -------------------------
# Vector stores / embeddings
# -------------------------
# NOTE: faiss-cpu often installs easier via conda on macOS (conda-forge). You
# can uncomment this line and try pip, but if it fails, use the conda steps below.
faiss-cpu==1.9.0.post1

# -------------------------
# PDF + ML (TF-IDF fallback)
# -------------------------
pypdf==4.3.1
scikit-learn==1.5.2
sentence-transformers
chromadb
langchain-huggingface
pdfplumber
langchain-text-splitters
tqdm
